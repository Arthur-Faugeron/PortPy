# returns

Functions for computing, transforming, and summarizing portfolio returns. These metrics cover the full return workflow, from converting prices into returns and rebuilding price series to calculating cumulative, annualized, excess, and benchmark-relative performance.

::: portpy.metrics.returns
    options:
      members:
        - simple_returns
        - log_returns
        - cumulative_returns
        - prices_from_returns
        - total_return
        - annualized_return
        - cagr
        - average_return
        - rebased_returns
        - excess_returns
        - active_returns
