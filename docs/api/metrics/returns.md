# returns

::: portpy.metrics.returns
    options:
      members:
        - simple_returns
        - log_returns
        - cumulative_returns
        - prices_from_returns
        - total_return
        - annualized_return
        - cagr
        - average_return
        - rebased_returns
        - excess_returns
        - active_returns
