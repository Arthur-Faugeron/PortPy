# construction

Constraint objects (re-exported from [`portpy.models.base`](./base.md)), plus the `optimize`/`build`/`efficient_frontier` recipe layer over [`portpy.models.optimization`](./optimization.md). `optimize`/`build`/`efficient_frontier` are also exposed directly on `Portfolio.models` for convenience - see [Construction, optimization & management](../../guide/models.md).

## Constraint objects

::: portpy.models.construction.constraints

## optimize / build / efficient_frontier

::: portpy.models.construction.builders
